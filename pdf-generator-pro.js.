/**
 * pdf-generator-pro.js - MODO DE PRUEBA DE HUMO
 * Este archivo se ha simplificado para diagnosticar un problema de carga.
 */

function generateProfessionalPDF(data) {
    // Esta es la funcion que el interceptor espera encontrar.
    console.log("SUCCESS [SMOKE TEST]: La funcion generateProfessionalPDF fue llamada.");
    alert("MOTOR DE PDF CARGADO CORRECTAMENTE (Modo de Prueba). Si ve esto, el archivo se esta cargando. El problema esta en el codigo del motor anterior, no en la carga.");
    
    // Devolver una promesa rechazada para que el proceso se detenga aqui.
    return Promise.reject("MODO DE PRUEBA DE HUMO: La generacion real de PDF esta deshabilitada.");
}

// Adjuntar la funcion al objeto window para que sea globalmente accesible
window.generateProfessionalPDF = generateProfessionalPDF;

console.log("SUCCESS [SMOKE TEST]: El archivo pdf-generator-pro.js ha sido cargado y parseado por el navegador.");
alert("SMOKE TEST: El archivo pdf-generator-pro.js ha sido cargado por el navegador.");
